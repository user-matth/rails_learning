<nav class="py-2 bg-light border-bottom w-100">
    <div class="container d-flex flex-wrap">
      <ul class="nav me-auto">
        <li class="nav-item"><a href="#" class="nav-link link-dark px-2 active fw-bold" aria-current="page">Rails Project</a></li>
        <li class="nav-item"><a href="#" class="nav-link link-dark px-2 active" aria-current="page">Home</a></li>
        <% if user_signed_in? %>
          <%= link_to "Sidekiq", sidekiq_web_path, :class => "nav-item nav-link link-dark px-2" %>
        <% end %>
      </ul>
      <ul class="nav">        
        <% if user_signed_in? %> 
            <%= link_to "Sign out", destroy_user_session_path, :method => :delete, :class => "nav-item nav-link link-dark px-2" %>
        <% else %>
            <%= link_to "Login", new_user_session_path, :class => "nav-item nav-link link-dark px-2" %>
        <% end %>
      </ul>
    </div>
</nav>